<%= form_with(model: tool, local: true) do |form| %>
  <% if @errors.present? %>
    <div id="error_explanation" class='red-text'>
      <b><%= pluralize(@errors.count, "error") %> prohibited this tool from being saved:</b>

      <ul>
        <% @errors.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <br>

  <div class="row">
    <div class="col s6">
      <div class="input-field">
        <%= form.label :name %>
        <%= form.text_field :name %>
      </div>
      <div class="input-field">
        <%= form.label :language %>
        <%= form.text_field :language %>
      </div>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn' %>
  </div>
<% end %>
